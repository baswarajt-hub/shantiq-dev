service cloud.firestore {
  match /databases/{database}/documents {

    // Example: users can only read and write their own profile
    match /users/{userId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }

    // Example: posts collection — users can read all posts, but only edit/delete their own
    match /posts/{postId} {
      allow read: if true;  // anyone can read posts
      allow create: if request.auth != null; // only logged-in users can create
      allow update, delete: if request.auth != null && request.auth.uid == resource.data.authorId;
    }

    // Deny all other access by default
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
